GLOBALPEDIA UI

Extract this ZIP into the root of your existing Next.js project (faaaah).
It replaces the app/page.tsx and app/globals.css files.

Then run:
  npm.cmd run dev

For Git:
  git add .
  git commit -m "feat: build GlobalPedia premium UI"
  git push
